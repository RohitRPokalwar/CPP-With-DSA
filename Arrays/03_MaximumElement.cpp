#include <iostream>
#include <limits.h>  //Add this Library
using namespace std;
int main(){
int maxi=INT_MIN;  // It must be a INT_MIN as we cannot take zero in this if array contains negative number it will give error
return 0;
}